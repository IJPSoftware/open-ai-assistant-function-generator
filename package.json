{
  "name": "open-ai-assistant-function-generator",
  "version": "1.0.6",
  "description": "Generate Open AI function schema based on ts files with functions",
  "main": "dist/index.js",
  "scripts": {
    "start": "ts-node ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "bin": {
    "openai-generator": "./dist/index.js"
  },
  "author": "Ivan",
  "license": "ISC",
  "dependencies": {
    "ts-json-schema-generator": "^1.5.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  },
  "resolutions": {}
}